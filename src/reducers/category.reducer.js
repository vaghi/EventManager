import * as types from '../constants/actionTypes/categoryActionTypes';

const initialState = {
}

export const categoryReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}
